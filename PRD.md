# VividSoul PRD

- 작성자: 이은정, 이고은
- 문서 버전: v2.0
- 작성일: 2026-07-30 (최초) / 2026-09-10 갱신

---

## 1. 한 줄 소개

서울 자치구와 여행 날짜, 관심사(카테고리)를 선택하면, 그날의 날씨에 따라 관심사별 숨겨진 명소와 현지 맛집을 추천해주는 반응형 웹앱.

---

## 2. 문제 정의

- 여행 계획을 세울 때 사람들은 보통 유명 관광지 위주의 정보만 얻게 되고, 그 지역의 숨겨진 명소나 현지인이 실제로 찾는 맛집 정보는 찾기 어렵다.
- 여행 당일 날씨에 따라 실내/실외 일정을 조정해야 하는데, 날씨 정보와 여행지 추천이 따로 분리되어 있어 사용자가 직접 종합 판단을 해야 하는 번거로움이 있다.
- 사용자마다 여행에서 중요하게 여기는 것(맛집, 역사, 자연, 쇼핑 등)이 다른데, 관심사와 무관하게 획일적인 추천만 받게 된다.
- 결과적으로 여행자는 "이 날씨에, 내가 관심 있는 분야 위주로, 이 지역에서 무엇을 하면 좋을지"를 한 번에 알려주는 서비스가 필요하다.

---

## 3. 핵심 기능

| 항목 | 내용 |
|---|---|
| 기능 | 관심사(카테고리)별 숨겨진 현지 명소/맛집 추천, 서울 축제·행사 안내, 여행 참고 정보(환율, 현위치) 제공 |
| 입력 (사용자가 무엇을 넣으면) | 가고 싶은 서울 자치구 선택(또는 현위치), 여행 날짜·일정 선택, 관심사(카테고리) 다중 선택 |
| 출력 (AI/앱이 무엇을 돌려주면) | 선택한 지역·날짜의 날씨 정보 → 관심사 카테고리별로 분리된 숨겨진 명소/맛집 추천 (카테고리 미선택 시 전체 카테고리 추천) |

### 3-1. 관심사(카테고리)

문화관광, 쇼핑, 숙박, 역사관광, 음식, 자연관광, 체험관광, 축제/공연/행사 — 8종 중 다중 선택 가능. 선택하지 않으면 8종 전체에 대해 추천한다.

### 3-2. 사용자 플로우

1. 사용자가 웹앱에 접속한다.
2. (선택) 현위치 버튼으로 자동 지역 인식을 시도하거나, 직접 서울 자치구를 선택한다.
3. 여행 날짜와 일정(당일치기~최대 6박)을 선택한다.
4. 관심 있는 카테고리를 하나 이상 선택한다(미선택 시 전체 카테고리로 추천).
5. 앱이 해당 지역·날짜의 날씨 정보를 조회한다.
6. 첫째 날 날씨를 고려해 선택한 카테고리별로 숨겨진 현지 명소(또는 맛집)를 추천한다.
7. 사용자는 카테고리별로 나뉜 추천 결과와 각 장소의 사진, 지도 링크를 확인한다.
8. "축제/공연/행사"를 선택했다면 서울 축제·행사 카드도 함께 확인한다.

---

## 4. 외부 데이터 / API (구현 완료)

| 구분 | 사용 API | 비고 |
|---|---|---|
| 지오코딩 / 역지오코딩 | 카카오맵(Local) API | 지역명 → 위경도, 좌표 → 주소(현위치 기능) |
| 날씨 | Open-Meteo | API 키 불필요, 오늘부터 최대 16일 이내 예보만 제공 |
| 추천 생성 (AI) | Gemini (`google-genai` SDK) | 카테고리별 명소/맛집 추천 문구 생성, JSON 스키마 강제 |
| 장소 사진 | 비짓서울 API 우선, 실패 시 Google Places API 폴백 | 서울 콘텐츠는 비짓서울 우선, 둘 다 실패하면 사진 없이 텍스트만 표시 |
| 관광 콘텐츠(축제 등) | 비짓서울 API | 키워드 검색으로 서울 축제·행사 카드 제공 |
| 환율 | 한국수출입은행 API | JPY/USD/CNY 환율 티커, 주말·공휴일은 최근 영업일 값으로 폴백 |

---

## 5. 완성 기준 (완료의 정의)

> "지역·날짜·관심사를 선택하면 관심사별로 분리된 숨겨진 명소/맛집이 추천된다."

- [x] 지역 선택 UI 동작 (서울 자치구 자동완성 + 현위치 버튼)
- [x] 날짜·일정 선택 UI 동작 (당일치기~최대 6박)
- [x] 관심사(카테고리) 다중 선택 UI 동작
- [x] 선택한 지역·날짜 기준 날씨 정보 표시
- [x] 관심사 카테고리별로 분리된 추천 섹션 표시 (미선택 시 전체 카테고리)
- [x] 각 추천 장소에 사진 및 지도 링크 표시
- [x] 서울 축제·행사 카드 표시 ("축제/공연/행사" 선택 시)
- [x] 환율 티커, 현위치, 다국어 선택 UI 노출
- [x] 반응형 레이아웃(모바일/데스크톱) 적용

---

## 6. 기술 스택 / 제약사항

- 프론트엔드: React + Vite
- 백엔드: Python 3.11 + Flask
- AI: Gemini — 반드시 `google-genai` SDK 사용 (구버전 `google-generativeai` 패키지 사용 금지)
- 서비스 지역: 서울 25개 자치구로 한정 (그 외 지역 입력 시 안내 후 차단)
- 화면은 반응형으로 구현

---

## 7. 향후 고려사항 (MVP 이후 범위)

- 사용자 로그인/여행 기록 저장
- 서울 외 지역으로 서비스 범위 확대
- 다중 지역/다일정 여행 코스 추천
- 사용자 리뷰·평점 기반 맛집 필터링
- 다국어 지원 완성 (현재 언어 선택 UI는 있으나 실제 번역 콘텐츠 연동은 미구현)
